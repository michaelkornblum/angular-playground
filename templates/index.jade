extends ./layouts/default.jade

block title
  title AngularJS Demo
  
block content
  .panel
    .page-header
      h1 AngularJS App
    h2(ng-controller="TodayCtrl" highlight="Saturday") Today is {{day || "(unknown)" | dayName}}
    h2(ng-controller="TomorrowCtrl" highlight="Sunday") Tomorrow is {{day || "(unknown)" | dayName}}
