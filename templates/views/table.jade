table.table
  thead
    tr
      th # 
      th Action  
      th Done
  tbody
    tr(ng-repeat="item in todos" ng-class="$odd ? 'odd' : 'even'")
      td {{$index + 1}}
      td(ng-repeat="prop in item") {{prop}}
